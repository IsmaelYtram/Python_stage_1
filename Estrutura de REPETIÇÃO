
#3. Estrutura de Repetição


#l) Faça um programa em python que imprima de 1 a 50 na tela.

count = 1   #objeto do tipo int, que vai fucionar como um contador
while count <= 50:  #Estrutura while, que vai executar o bloco enquanto o teste logico for verdadeiro
    print(count)    #vamos printar o contador
    count = count + 1   #estamos incrementando +1 no contador

#m) Elabore um código que imprima uma sequência de 1 até 100 somente com os números pares dessa sequência.

count = 1   #objeto que vai funcionar como um contador
while count <= 100:     #Estrutura while, que vai executar o bloco abaixo enquanto for verdadeiro o teste logico 
    if(count %2 == 0):  #condicional if, caso resto por 2 seja 0 vai executar o bloco abaixo 
        print(count)    #vai printar o contador
    count = count + 1   #vamos incrementar no contador 

#n) Crie um algoritmo em Python que, dado um número informado pelo usuário, imprima a tabuada dele de 1 a 10. Use o formato de apresentação.

num = int(input("Informe um valor:"))   #objeto tipo int, função input para receber os dados 

for i in range(1,11,1):     #Estrutura de Repetição for, começa com 1, termina no 10 e incrementa em 1
    print(i,"x",num,"=",i*num) #vamos exeibir

#o) Desenvolva um programa em Python, que imprima os números pares entre 1 e 100.

count = 1   #objeto que funciona como contador 

while count <= 100:     #Estrutura de Repetição while, executa o bloco enquanto teste logico for verdadeiro
    if(count %2 == 0):  #condicional if, se for verdadeiro executa o bloco abaixo 
        print(count)
    count = count + 1   #vamos incrementa + 1 no count

#p) Faça um script em Python que imprima na tela os múltiplos de 5 entre 1 e 100.

for i in range(1,101,1):    #Estrutura for, começa no 1, vai ate 100 e incrementa em +1 
    if(i %5 == 0):  #condicional if, caso verdadeiro executar o bloco abaixo 
        print(i)

#q) Desenvolva um código em Python que calcule a média aritmética entre 1 e 20.

num = 20    #objeto tipo int que vai sofrer decremento
for i in range(1,21,1): #Estrutura for, começa em 1, vai ate 20 e incrementa em 1
    print("Média aritmética entre",i,"e",num,"é:",(i+num)/2)    #executar o print e a conta dentro dele
    num = num - 1   #vai decrementar o num 

#r) Faça um script em Python que calcule o fatorial de um número 𝑛 informado pelo usuário.
#O fatorial de um número é calculado pela multiplicação desse número por todos os seus 
#antecessores até chegar ao número 1

num = int(input("Informe um número:")) #objeto tipo num, função input para receber os dados
fat = 1 #objeto que vai funcionar como o primeiro elemento fatorial 

for i in range(1,num+1,1):  #Estrutura de Repetição for, começa em 1, vai ate o numero informado pelo usuário e incrementa em 1 
    fat = fat*i #o fat vai ser multiplicado ate o limite do for atingir 
print("O fatorial do número é:",fat) #exibimos o resultado fatorial 
